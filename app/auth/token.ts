import tokenResponse from "../models/auth/tokenResponse";
import userAuthInfo from "../models/auth/userAuthInfo";

const Token = {
    createJWT(userInfo:userAuthInfo) {
        
    }
}


export default Token